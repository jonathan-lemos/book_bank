export const cover = (id: string) => `${window.location.origin}/api/books/cover/${id}`;
export const thumbnail = (id: string) => `${window.location.origin}/api/books/thumbnail/${id}`;
export const download = (id: string) => `${window.location.origin}/api/books/download/${id}`;
